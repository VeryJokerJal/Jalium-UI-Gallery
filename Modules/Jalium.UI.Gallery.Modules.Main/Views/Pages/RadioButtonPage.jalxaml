<Page xmlns="http://schemas.jalium.ui/2024"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      x:Class="Jalium.UI.Gallery.Modules.Main.Views.Pages.RadioButtonPage"
      Title="RadioButton">

    <StackPanel Orientation="Vertical">
        <!-- Page Title -->
        <TextBlock Text="RadioButton"
                   FontSize="32"
                   Foreground="{DynamicResource GalleryTextPrimary}"
                   Margin="0,0,0,8"/>

        <!-- Description -->
        <TextBlock Text="A button that can be selected but not cleared by a user. RadioButtons in the same group are mutually exclusive."
                   FontSize="14"
                   Foreground="{DynamicResource GalleryTextTertiary}"
                   Margin="0,0,0,24"/>

        <!-- Interactive Demo Card -->
        <Border Background="{DynamicResource GalleryCardBackground}"
                BorderBrush="{DynamicResource GalleryBorderDefault}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="20"
                Margin="0,0,0,16">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Interactive Demo"
                           FontSize="16"
                           Foreground="{DynamicResource GalleryTextPrimary}"
                           Margin="0,0,0,4"/>
                <TextBlock Text="Select your favorite color."
                           FontSize="12"
                           Foreground="{DynamicResource GalleryTextMuted}"
                           Margin="0,0,0,16"/>

                <Border Background="{DynamicResource GalleryCardInnerBackground}"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Orientation="Vertical">
                        <RadioButton x:Name="RadioRed" GroupName="Colors" Margin="0,0,0,8" Foreground="{DynamicResource GalleryTextPrimary}">
                            <TextBlock Text="Red" Foreground="{DynamicResource GalleryTextPrimary}" FontSize="14"/>
                        </RadioButton>
                        <RadioButton x:Name="RadioGreen" GroupName="Colors" Margin="0,0,0,8" Foreground="{DynamicResource GalleryTextPrimary}">
                            <TextBlock Text="Green" Foreground="{DynamicResource GalleryTextPrimary}" FontSize="14"/>
                        </RadioButton>
                        <RadioButton x:Name="RadioBlue" GroupName="Colors" IsChecked="True" Margin="0,0,0,8" Foreground="{DynamicResource GalleryTextPrimary}">
                            <TextBlock Text="Blue" Foreground="{DynamicResource GalleryTextPrimary}" FontSize="14"/>
                        </RadioButton>
                        <TextBlock x:Name="RadioStatus"
                                   Text="Selected: Blue"
                                   Foreground="{DynamicResource GalleryTextTertiary}"
                                   FontSize="12"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Grouped RadioButtons Card -->
        <Border Background="{DynamicResource GalleryCardBackground}"
                BorderBrush="{DynamicResource GalleryBorderDefault}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="20"
                Margin="0,0,0,16">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Grouped RadioButtons"
                           FontSize="16"
                           Foreground="{DynamicResource GalleryTextPrimary}"
                           Margin="0,0,0,4"/>
                <TextBlock Text="RadioButtons can be organized into separate groups."
                           FontSize="12"
                           Foreground="{DynamicResource GalleryTextMuted}"
                           Margin="0,0,0,16"/>

                <Border Background="{DynamicResource GalleryCardInnerBackground}"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Orientation="Horizontal">
                        <StackPanel Orientation="Vertical" Margin="0,0,32,0">
                            <TextBlock Text="Size" Foreground="{DynamicResource GalleryTextTertiary}" FontSize="12" Margin="0,0,0,8"/>
                            <RadioButton GroupName="Size" Margin="0,0,0,4" Foreground="{DynamicResource GalleryTextPrimary}">
                                <TextBlock Text="Small" Foreground="{DynamicResource GalleryTextPrimary}" FontSize="14"/>
                            </RadioButton>
                            <RadioButton GroupName="Size" IsChecked="True" Margin="0,0,0,4" Foreground="{DynamicResource GalleryTextPrimary}">
                                <TextBlock Text="Medium" Foreground="{DynamicResource GalleryTextPrimary}" FontSize="14"/>
                            </RadioButton>
                            <RadioButton GroupName="Size" Foreground="{DynamicResource GalleryTextPrimary}">
                                <TextBlock Text="Large" Foreground="{DynamicResource GalleryTextPrimary}" FontSize="14"/>
                            </RadioButton>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Speed" Foreground="{DynamicResource GalleryTextTertiary}" FontSize="12" Margin="0,0,0,8"/>
                            <RadioButton GroupName="Speed" Margin="0,0,0,4" Foreground="{DynamicResource GalleryTextPrimary}">
                                <TextBlock Text="Slow" Foreground="{DynamicResource GalleryTextPrimary}" FontSize="14"/>
                            </RadioButton>
                            <RadioButton GroupName="Speed" IsChecked="True" Margin="0,0,0,4" Foreground="{DynamicResource GalleryTextPrimary}">
                                <TextBlock Text="Normal" Foreground="{DynamicResource GalleryTextPrimary}" FontSize="14"/>
                            </RadioButton>
                            <RadioButton GroupName="Speed" Foreground="{DynamicResource GalleryTextPrimary}">
                                <TextBlock Text="Fast" Foreground="{DynamicResource GalleryTextPrimary}" FontSize="14"/>
                            </RadioButton>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
        <!-- XAML Code Example -->
        <Border Background="{DynamicResource GalleryCardBackground}"
                BorderBrush="{DynamicResource GalleryBorderDefault}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="20"
                Margin="0,0,0,16">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="XAML Code Example"
                           FontSize="16"
                           Foreground="{DynamicResource GalleryTextPrimary}"
                           Margin="0,0,0,16"/>
                <Border Background="{DynamicResource GalleryCardInnerBackground}"
                        CornerRadius="8"
                        Padding="0"
                        HorizontalAlignment="Stretch">
                    <EditControl x:Name="XamlCodeEditor"
                                 Height="200"
                                 HorizontalAlignment="Stretch"
                                 IsReadOnly="True"
                                 ShowLineNumbers="True"
                                 FontSize="13"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- C# Code Example -->
        <Border Background="{DynamicResource GalleryCardBackground}"
                BorderBrush="{DynamicResource GalleryBorderDefault}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="20"
                Margin="0,0,0,16">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="C# Code Example"
                           FontSize="16"
                           Foreground="{DynamicResource GalleryTextPrimary}"
                           Margin="0,0,0,16"/>
                <Border Background="{DynamicResource GalleryCardInnerBackground}"
                        CornerRadius="8"
                        Padding="0"
                        HorizontalAlignment="Stretch">
                    <EditControl x:Name="CSharpCodeEditor"
                                 Height="200"
                                 HorizontalAlignment="Stretch"
                                 IsReadOnly="True"
                                 ShowLineNumbers="True"
                                 FontSize="13"/>
                </Border>
            </StackPanel>
        </Border>
    </StackPanel>
</Page>
